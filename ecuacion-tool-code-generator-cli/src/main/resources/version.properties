# This module's own version, read via VersionUtil.getVersion(""). Kept here rather than in
# ecuacion-tool-code-generator-core because that module is also depended on by
# ecuacion-references-tool-code-generator, which needs an unambiguous classpath for its own
# version.properties (see the comment on ecuacion-tool-code-generator-core's pom.xml
# <resources> for the full reasoning).
version=@project.version@
