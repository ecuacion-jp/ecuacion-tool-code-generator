package jp.ecuacion.tool.codegenerator.core.generator.annotation.validator;

/** Enum identifying the source pattern type used when generating a {@code @Pattern} validator. */
public enum PatternOfRegExpEnum {
  DATA_PTN, REG_EXP, PROHIBITED
}
